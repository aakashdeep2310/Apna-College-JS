// 2nd Array Method
// Filter Method
//  Creates a new array of elements that give true for a condition/filter.
// Eg: all even elements

//syntx
let newArr = arr.filter((val) => {
    return val % 2 == 0 // if return true value will store in new Array else not

})

 